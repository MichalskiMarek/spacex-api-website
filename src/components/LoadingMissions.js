import React from "react";
import styled from "styled-components";

const StyledDiv = styled.div`
  margin-top: 50px;
  font-size: 20px;
`;

const LoadingMissions = props => {
  return <StyledDiv>Loading missions...</StyledDiv>;
};

export default LoadingMissions;
